# Frontend Environment Variables

# API Configuration (used in frontend code)
VITE_API_BASE_URL=http://localhost:8081/api
VITE_API_URL=http://localhost:8081

# Note: Backend currently provides these endpoints:
# - POST /createRoom
# - GET /room/{id}  
# - WebSocket /ws
# The complex /api/tenant/* endpoints in frontend don't exist in current backend